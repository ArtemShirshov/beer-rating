import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  root: {
    height: 60,
    lineHeight: '60px',
  },
  icon: {
    marginRight: 8,
    top: 4,
    position: 'relative',
  },
});
